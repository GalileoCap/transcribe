# Environment variable loading
python-dotenv

# Core transcription (Apple Silicon — uses Metal via MLX)
mlx-whisper

# Speaker diarization (requires free HuggingFace account + token)
pyannote.audio

# PyTorch — pip version has MPS support on Apple Silicon
torch
torchaudio
